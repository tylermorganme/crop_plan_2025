import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Crop Planner | Seed Mixes',
};

export default function SeedMixesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
