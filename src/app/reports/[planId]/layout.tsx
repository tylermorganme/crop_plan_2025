import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Crop Planner | Reports',
};

export default function ReportsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
