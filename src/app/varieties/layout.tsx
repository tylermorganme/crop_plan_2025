import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Crop Planner | Varieties',
};

export default function VarietiesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
