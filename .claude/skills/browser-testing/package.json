{
  "name": "browser-testing-skill",
  "version": "1.0.0",
  "description": "Browser automation skill for Claude Code using Playwright",
  "private": true,
  "scripts": {
    "start": "node scripts/server.js",
    "setup": "npm install && npx playwright install chromium && node scripts/setup.js"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
